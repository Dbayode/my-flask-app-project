# Flask Dockerization Project

A reproducible, containerized Flask application using Docker.

## Prerequisites
- Docker Engine / Docker Desktop installed.

## Build and Run Instructions

1. **Build the image:**
   ```bash
   docker build -t flask-docker-app:1.0 .
